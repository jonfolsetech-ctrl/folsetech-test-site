version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd folse-tech-nextjs && npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: folse-tech-nextjs/out
    files:
      - '**/*'
  cache:
    paths:
      - 'folse-tech-nextjs/node_modules/**/*'
